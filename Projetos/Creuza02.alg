algoritmo "Creuza02"
// Professor(a): Anderson de Jesus
// Autor(a): Danilo Ramos

var
   reais, dolares: real
inicio
      escreva("Quantos Reais eu tenho? R$ ")
      leia(reais)

      dolares <- reais/2.22
      escreva("Com R$", reais, " posso comprar US$", dolares)

fimalgoritmo
