algoritmo "semnome"
// Professor(a): Anderson de Jesus
// Autor(a): Danilo Ramos

var
   a, b, c: inteiro
inicio
      a <- 2
      b <- 3
      c <- 5
      
      escreva(nao ((c=b) e (a<b)))
fimalgoritmo
